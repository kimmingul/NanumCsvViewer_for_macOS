# Nanum CSV Viewer v1.7.4

## Highlights

- Fixed the app menu title beside the Apple menu so `Nanum CSV Viewer` displays without overlapping an icon.
- Added a custom About window for `Nanum CSV Viewer 정보`.
- Added app icon, version, copyright, developer name, and affiliation details to the About window.
- Tuned About window typography to a compact macOS-style size.
- Added `CFBundleDisplayName` to the app bundle metadata and bumped the bundle version to `1.7.4(174)`.

## Validation

- `swift test --filter AppMenuTests`
- `swift test`
- `SKIP_NOTARIZE=1 Scripts/release-app.sh`
- `Scripts/create-dmg.sh`

## Distribution

- Developer ID signed app bundle: `NanumCsvViewerMac/dist/Nanum CSV Viewer.app`
- Developer ID signed DMG artifact: `NanumCsvViewerMac/dist/Nanum-CSV-Viewer-v1.7.4.dmg`
- SHA-256: `4ebd85a74a09cdec078cce6199d3b2681f213a6d0b1fc064c064a7a0b929aaba`

Notarization was not included in this local release because no `notarytool` credentials were configured in the keychain or environment.
